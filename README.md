# dns
[dnscontrol](https://github.com/StackExchange/dnscontrol) management for my personal domains

# Dependencies for local runs
* Docker
* `op` -- 1Password CLI
* `direnv`

# Usage
`make` to preview changes. `make push` to apply.`
